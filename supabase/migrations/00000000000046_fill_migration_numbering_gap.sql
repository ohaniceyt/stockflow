-- This migration intentionally fills the numbering gap between 00044 and 00047.
-- It documents that no schema change was required here; the gap existed due to
-- previously squashed or reordered development migrations.
-- Future migrations should continue the sequential zero-padded numbering.

SELECT 1;
